import React from "react";

import "../forms/SimpleForm";
import SimpleForm from "../forms/SimpleForm";

const InputEmployee = (setAuth) => {
  return <SimpleForm isNew={true} />;
};

export default InputEmployee;
